//Escreva um algoritmo que lÊ dois valores boleanos (lógicos) e então determine se ambos são VERDADEIROS ou FALSOS.

let x = 20
let y = 20

if(x === y) {
    document.write(`${x} e ${y} são verdadeiros`)
} else {
    document.write(`${x} e ${y} são falsos`)
}
