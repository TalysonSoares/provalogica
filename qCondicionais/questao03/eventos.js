//Faça um algoritmo para receber um número qualquer e informar se é par ou impar.
// First try

let x = 4
if (x % 2 === 0) {
    document.write (`${x} é par`)
} else {
    document.write (`${x} é impar`)
}